{
  "name": "react-native-versioned-components",
  "packageManager": "yarn@1.22.1",
  "private": true,
  "workspaces": {
    "packages": [
      "app-mobile",
      "packages/*",
      "internals/*"
    ]
  },
  "scripts": {
    "android": "yarn workspace app-mobile android",
    "start": "yarn workspace app-mobile start",
    "flow": "yarn workspace app-mobile flow",
    "test": "yarn workspace app-mobile test",
    "lint": "yarn workspace app-mobile lint"
  },
  "devDependencies": {
    "@rnx-kit/metro-config": "^1.2.38",
    "@rnx-kit/metro-resolver-symlinks": "^0.1.21",
    "ts-node": "^10.8.0",
    "typescript": "^4.7.2"
  }
}
